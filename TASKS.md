# Tasks

## Active

- [ ] **Repair Figma sync contract** — update `figma-sync-order.json` + `figma-agent-tasks.md`; fix MISSING route frames; separate canonical, redirect-history, utility/internal pages
- [x] ~~**Finish `/profile` canonical desktop cleanup**~~ — in progress: complete content-lane cleanup on `1:4411`; remove narrow-layout offsets

## Waiting On

- [ ] **Define shared shell anchors** — document stable node IDs for sidebar, page chrome header, content frame — *waiting on: Figma sync contract repair*
- [ ] **Decide `/opportunities` canonical form** — desktop canonical vs mobile reference; resize/reflow `1:2333` or record policy — *waiting on: sync-contract repair*
- [ ] **Normalize `/applications` shell** — rebuild `1:3176` from PageCanvas into canonical shell; `/tracker` + `/kanban` → redirect-history — *waiting on: shared shell anchors*
- [ ] **Normalize `/analysis` shell** — rebuild `1:5116` into canonical shell with meaningful content-frame naming — *waiting on: shared shell anchors*
- [ ] **Normalize `/documents` shell** — rebuild `1:5490` into canonical shell; collapse `/docs` + `/editor` → redirect-history — *waiting on: shared shell anchors*
- [ ] **Confirm code-target nodes for near-ready screens** — `/` `/auth` `/onboarding` `/dashboard` `/apply` `/generation` `/settings`: define actual code-target node/frame, record alias policy — *waiting on: sync-contract repair*
- [ ] **Collapse redirect-history pages** — for each alias: archive, fold into canonical state/tab, convert to annotation, or remove — *waiting on: canonical shell work per route family*
- [ ] **Design-system readiness pass** — verify style-guide anchors, shell primitives, token naming, asset annotations before broad code extraction — *waiting on: shared shell anchors*

## Someday

## Done

- [x] ~~**Planning surface consolidation**~~ — archived ~40 stale docs, created SPRINT_BRIEF.md + DECISIONS.md, added governance rule to CLAUDE.md (2026-04-17)
- [x] ~~**Install andrej-karpathy-skills plugin**~~ (2026-04-17)
