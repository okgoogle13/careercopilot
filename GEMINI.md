@./AGENTS.md

# Gemini CLI

- Use `AGENTS.md` as the canonical repository instruction source.
- Keep Gemini-specific global behavior in `~/.gemini/settings.json` rather than duplicating repo rules here.
