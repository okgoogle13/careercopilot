// SkillBreakdownCard component was removed; keep a skipped suite as a reminder to restore coverage.
describe.skip('SkillBreakdownCard', () => {
  it('needs replacement coverage when the component returns', () => {
    expect(true).toBe(true);
  });
});
