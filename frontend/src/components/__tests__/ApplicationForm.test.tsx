// ApplicationForm component was removed; keep a skipped suite as a reminder to restore coverage.
describe.skip('ApplicationForm', () => {
  it('needs replacement coverage when the component returns', () => {
    expect(true).toBe(true);
  });
});
