# Prototype Quarantine Zone
Logic migration to Zustand/TanStack Query pending.
Uses `PrototypeAdapter` for visual consistency.
`useState`/`useEffect` and generic naming are permitted.
This is a 'Ready-to-Receive' zone for prototype harvesting.
