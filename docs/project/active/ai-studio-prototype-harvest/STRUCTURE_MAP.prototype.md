# STRUCTURE_MAP — AI Studio Prototype ↔ Main Repo

**Date:** 2026-03-18
**Blueprint ref:** `control/blueprint-ai-studio-integration.md` (Option 3, Tracks A/B/C)
**Prototype:** `/Users/okgoogle13/Projects/prototype_v2.0`
**Main repo:** `careercopilot` (branch: `feat/migration-cleanup-jobs-opportunities`)

---

## 1 · Routes & Navigation Flows

> The prototype is a **tab-based SPA** — 4 tabs controlled by `activeTab` state in `App.tsx`. The main repo uses `react-router-dom` + `MigratedRouteLayout`. This is the highest structural gap, but it is also the easiest tweak.

| Item | Location | Role | Prototype Shape | Target Shape (per blueprint) | Gap Notes | Effort (tweak) | Impact (on migration) | Harvest Category | Tweak Sketch | AI Studio Prompt Stub |
|------|----------|------|-----------------|------------------------------|-----------|:-:|:-:|---|---|---|
| `App.tsx` navigation shell | `proto:App.tsx` | Top-level SPA tab controller | 4-tab state machine: `'workspace' \| 'profile' \| 'past' \| 'components'`. Renders `AppShell` with conditional page slots. | Per C1: pages wired via `react-router-dom` under `MigratedRouteLayout`. No shared state for active route. | Prototype uses local `activeTab` state; target uses `<Route>` paths. No router imports in prototype. | 2 | 3 | `IGNORE` | — | — |
| `AppShell` | `proto:src/components/layout/AppShell` | Layout shell + tab nav | Renders sidebar/header + tab content slot | Per C1: `MigratedRouteLayout` renders `<Outlet>` for route pages. | Shell navigation pattern not reusable as-is; replace with `MigratedRouteLayout`. Harvest zero code. | 1 | 1 | `IGNORE` | — | — |
| `/workspace` → `/analysis` | `proto:src/pages/ApplicationWorkspacePage.tsx` | Entry point to analysis flow | Thin shell: renders `ValidationDashboard` or sub-views | Per C2/C3: `AnalysisPage.tsx` is the target — already exists, wired via router | Page shell not needed; `ValidationDashboard` content is harvested separately. | 1 | 1 | `IGNORE` | — | — |

---

## 2 · Page & Layout Components

> The prototype's `ValidationDashboard.tsx` (72KB, ~1,238 lines) is the primary page-level concern. `Dashboard.tsx` is already token-compliant and a lower priority.

| Item | Location | Role | Prototype Shape | Target Shape (per blueprint) | Gap Notes | Effort (tweak) | Impact (on migration) | Harvest Category | Flags | Tweak Sketch | AI Studio Prompt Stub |
|------|----------|------|-----------------|------------------------------|-----------|:-:|:-:|---|---|---|---|
| `ValidationDashboard.tsx` | `proto:components/ValidationDashboard.tsx` | Full analysis workspace (achievements, KSCs, ATS scoring, cover letter) | 1,238-line monolith. Handles 6+ concerns: profile editing, achievement display, KSC editing, ATS scoring, document generation, Firebase saves. Uses direct `geminiService` calls. Raw Tailwind throughout. | Per Sprint 2 (deferred): decomposed into 4–6 sub-components, KR Solidarity tokens, TanStack Query mutations replacing Firebase/geminiService calls. | (1) Monolith must be decomposed first. (2) All raw Tailwind colors (`bg-gray-*`, `text-emerald-*`, etc.) must be replaced. (3) `geminiService` calls replaced with `useMutation`. (4) Firebase save replaced with `/api/v1/profile/save`. | 3 | 5 | `TWEAK_FIRST` | `HIGH_IMPACT` `BLOCKER_FOR_TRACK_C` | • Feed to Gemini: "Decompose into sub-components each ≤200 lines, single responsibility" • Batch-replace raw Tailwind with `--sys-color-*` tokens • Replace `geminiService` calls with a `onRequestAI(type, payload)` callback prop — defer actual mutation wiring to Track C | `"Decompose ValidationDashboard into 4–6 sub-components each ≤200 lines. Replace all raw Tailwind color utilities with KR Solidarity v6.1 CSS variables per the token map. Replace all geminiService.* calls with an onRequestAI(type, payload) callback prop. Replace Firebase save calls with an onSaveProfile(data) callback prop. Keep all EditableField patterns and existing logic intact. Output each file separately."` |
| `Dashboard.tsx` | `proto:components/Dashboard.tsx` | Welcome/entry gateway | Fully token-compliant. Uses `--sys-color-*` semantic tokens and `--sys-shape-blockRiot*`. 83 lines. Clean. | Not in target scope — main repo has its own `Dashboard.tsx`. | No token debt. Not a migration target. | 1 | 1 | `IGNORE` | — | — | — |
| `MatchDashboard.tsx` | `proto:components/MatchDashboard.tsx` | Match analysis + document generation UI | 34KB, mixed Tailwind/tokens. Generates tailored resume, cover letter, KSC drafts. Calls `geminiService` directly. | Not in Sprint 1 blueprint scope. | Large component, direct AI calls, no route target yet. | 4 | 2 | `IGNORE` | — | — | — |

---

## 3 · Analysis & Scoring UI Components

> These are the highest-leverage items. Both components have correct props and correct logic — the only gap is raw Tailwind color utilities. This is a clean TWEAK_FIRST with the re-skin prompt being the tweak itself.

| Item | Location | Role | Prototype Shape | Target Shape (per blueprint) | Gap Notes | Effort (tweak) | Impact (on migration) | Harvest Category | Flags | Tweak Sketch | AI Studio Prompt Stub |
|------|----------|------|-----------------|------------------------------|-----------|:-:|:-:|---|---|---|---|
| `ATSScoreCard.tsx` | `proto:components/ATSScoreCard.tsx` | Renders ATS score gauge + breakdown grid | Named export ✅. Props: `{ score: ATSScoreResult \| null, isCalculating: boolean, documentType: DocumentType }` ✅. Has `framer-motion` (scale/opacity entrance) ✅. Import from `'../types'`. Raw Tailwind: `bg-gray-800`, `text-white`, `text-gray-400`, `text-emerald-500`, `stroke-emerald-*`, `text-amber-500`, `text-rose-500`, `rounded-xl`. SVG ring uses hardcoded `stroke="#374151"`. | Per B1: saved to `frontend/src/features/analysis/components/ATSScoreCard.tsx`. Import types from `'../../../types/analysis'`. All Tailwind replaced with `--sys-color-*` CSS vars. Named export kept. | Props interface is already target-aligned. Type import path needs updating. 12+ hardcoded Tailwind color utilities + 1 hardcoded hex SVG stroke color. Border radius needs shape token. | 1 | 4 | `TWEAK_FIRST` | `HIGH_IMPACT` `LOW_EFFORT` `BLOCKER_FOR_TRACK_B` | • Change import from `'../types'` → `'../../../types/analysis'` • Replace 12 raw Tailwind utilities with CSS var equivalents per B1 token map • Replace hardcoded SVG stroke hex with `var(--sys-color-charcoalBackground-steps-2)` | `"[Paste B1 prompt from blueprint exactly — already specifies 100% of required transforms including token map, border radius, import path, and framer-motion preservation]"` |
| `AuditDisplay.tsx` | `proto:components/AuditDisplay.tsx` | Renders 10-Second Recruiter Scan + violation list + recommendations | Named const export ✅. Props: `{ audit: DocumentAudit, title: string }` ✅. No framer-motion (none required). Import from `'../types'`. Raw Tailwind: `bg-gray-900/50`, `bg-gray-800`, `border-gray-700`, `text-white`, `text-gray-300`, `text-gray-500`, `border-cyan-500`, `bg-cyan-900/10`, `text-cyan-400`, `text-cyan-500`, `text-green-400`, `text-red-400`, `border-red-500/30`, `bg-red-900/10`, `text-amber-400`, `border-amber-500/30`, `bg-amber-900/10`, `text-blue-400`, `border-blue-500/30`, `bg-blue-900/10`, `rounded-xl`, `rounded-r-lg`. | Per B2: saved to `frontend/src/features/analysis/components/AuditDisplay.tsx`. Import from `'../../../types/analysis'`. All Tailwind replaced with `--sys-color-*` CSS vars. Named export kept. "10-Second Recruiter Scan" label and italic text preserved. | Same as ATSScoreCard — no logic changes needed, only token re-skin + import path update. 20+ raw Tailwind color utilities. | 1 | 4 | `TWEAK_FIRST` | `HIGH_IMPACT` `LOW_EFFORT` `BLOCKER_FOR_TRACK_B` | • Change import from `'../types'` → `'../../../types/analysis'` • Replace 20+ raw Tailwind utilities with CSS var equivalents per B2 token map • Keep "10-Second Recruiter Scan" heading and italic quote text unchanged | `"[Paste B2 prompt from blueprint exactly — already specifies 100% of required transforms]"` |
| `CoverLetterSpecificMetrics.tsx` | `proto:components/CoverLetterSpecificMetrics.tsx` | Cover letter ATS metric breakdown | Renders `CoverLetterScoreResult` breakdown. Raw Tailwind mixed with some tokens. Props: `{ score: CoverLetterScoreResult }` | Not in Sprint 1 blueprint. Potential Sprint 2 add-on for `/analysis` cover letter surface. | No blueprint target yet. Low priority. | 2 | 2 | `IGNORE` | — | — | — |

---

## 4 · Backend Prompt Contracts & Data Shapes

> `types.ts` and `ingestion_prompts.md` are the two highest-value pure-harvest assets in the repo.

| Item | Location | Role | Prototype Shape | Target Shape (per blueprint) | Gap Notes | Effort (tweak) | Impact (on migration) | Harvest Category | Flags | Tweak Sketch | AI Studio Prompt Stub |
|------|----------|------|-----------------|------------------------------|-----------|:-:|:-:|---|---|---|---|
| `types.ts` — `StructuredAchievement` + `KSCResponse` | `proto:types.ts` (L57–L101) | ANMOS data schema with `Needs_Review_Flag` + `Improvement_Suggestions` | `StructuredAchievement`: 11 fields including `needs_review_flag: boolean` and `improvement_suggestions: { Action_Verb?, Noun_Task?, Metric?, Strategy?, Outcome? }`. `KSCResponse`: includes `STAR_Feedback: string` and `Improvement_Suggestions: { Situation?, Task?, Action?, Result?}`. | Per A1: Python Pydantic equivalent — `AchievementSuggestions` model + fields added to `StructuredAchievement`. `star_feedback` + `improvement_suggestions` fields added to `KSCResponse`. | Type field names use PascalCase (prototype TS) vs snake_case (target Python). Pure mechanical transcription — no logic gaps. | 1 | 5 | `HARVEST_NOW` | `HIGH_IMPACT` `LOW_EFFORT` `BLOCKER_FOR_TRACK_A` | — | — |
| `types.ts` — `ATSScoreResult`, `DocumentAudit`, `AuditViolation`, `DocumentType` | `proto:types.ts` (L146–L210) | Core ATS + audit type definitions | `ATSScoreResult`: `overallScore`, `breakdown` (5 required + 3 optional fields), `matchedKeywords`, `missingKeywords`, `suggestions`, `keywordDensity`. `DocumentAudit`: `overallScore`, `scanSimulation`, `violations[]`, `recommendations[]`. `AuditViolation`: `ruleId`, `severity`, `message`, `location?`. `DocumentType`: `'resume' \| 'coverLetter'`. | Per B3: create `frontend/src/types/analysis.ts` — exact camelCase mirrors of these types. Blueprint B3 spec is already written to match these exactly. | **Zero gap.** Blueprint B3 was designed from these types. Copy verbatim. | 1 | 5 | `HARVEST_NOW` | `HIGH_IMPACT` `LOW_EFFORT` `BLOCKER_FOR_TRACK_B` | — | — |
| `ingestion_prompts.md` — DEEP STAR CRITIQUE | `proto:backend/prompts/ingestion_prompts.md` (L13–L31) | DEEP STAR heuristics: Vague Language Audit, Quantification Gap, Detail Deficiency, STAR_Feedback, Improvement_Suggestions | 4 audit rules as natural language instructions. Includes specific weak verb list ("assisted," "helped," "involved in") and power verb replacements ("orchestrated," "standardized," "pioneered"). Includes `[Insert Number here]` placeholder instruction. | Per A2: appended to existing `INGESTION_PROMPT` in `ingestion_flow.py` as additional numbered steps (after current step 4). | Must be **transcribed into Python f-string** format and merged with existing prompt. Field name casing: `Needs_Review_Flag` (proto) → `needs_review_flag` (Python). Tweak in prototype: rename casing to snake_case before harvest to reduce A2 transcription errors. | 1 | 4 | `TWEAK_FIRST` | `HIGH_IMPACT` `LOW_EFFORT` `BLOCKER_FOR_TRACK_A` | • Find/replace all `PascalCase` field names with `snake_case` in the prompt text (e.g. `Needs_Review_Flag` → `needs_review_flag`, `STAR_Feedback` → `star_feedback`) • Remove "Australian Community Services" domain-specific references (Section 4, Step 6 Subtype Tagging) or flag for IP review | `"In ingestion_prompts.md, find every reference to a data field name (e.g. Needs_Review_Flag, STAR_Feedback, Improvement_Suggestions, Action_Verb) and rewrite it in snake_case (needs_review_flag, star_feedback, improvement_suggestions, action_verb). Keep all logic, verb lists, and audit rules unchanged."` |
| `ingestion_prompts.md` — Strategic Tagging, STAR Refinement, Achievement Field Optimization | `proto:backend/prompts/ingestion_prompts.md` (L43–L85) | 3 secondary prompt templates with `{templated_variables}` | Python-style `{variable}` placeholders already used. 3 separate prompt sections. | Per A2: not directly in scope — these are secondary flows that feed the AI capability library, not the primary ingestion flow. | Clean prompt templates, ready to add to `genkit_flows/` as separate Genkit tools. No structural gaps. | 1 | 3 | `HARVEST_NOW` | — | — | — |
| `AppState` enum | `proto:types.ts` (L175–L181) | 5-state FSM: `IDLE \| PROCESSING \| VALIDATING \| JOB_EXTRACTION \| ERROR` | Numeric enum (no string values). | Per C4: optional TypeScript enum in `AnalysisPage.tsx` or shared types. Blueprint already specifies identical enum values. | Numeric enum → blueprint prefers string literal enum (`IDLE = 'IDLE'`). One-line tweak. | 1 | 2 | `TWEAK_FIRST` | `LOW_EFFORT` | • Change from numeric to string-literal enum: `IDLE = 'IDLE'` etc. | `"Change AppState enum from numeric to string-literal style: IDLE = 'IDLE', PROCESSING = 'PROCESSING', etc."` |

---

## Priority Ranking — TWEAK_FIRST Items

| Priority | Item | Combo | Estimated time in AI Studio |
|----------|------|-------|----------------------------|
| 🔴 Critical | `types.ts` — `ATSScoreResult` / `DocumentAudit` blocks | `HARVEST_NOW` + `BLOCKER_FOR_TRACK_B` | 0 min (copy-paste) |
| 🔴 Critical | `types.ts` — ANMOS types | `HARVEST_NOW` + `BLOCKER_FOR_TRACK_A` | 0 min (copy-paste) |
| 🟠 High | `ATSScoreCard.tsx` token re-skin | `TWEAK_FIRST` + `LOW_EFFORT` + `HIGH_IMPACT` + `BLOCKER_FOR_TRACK_B` | 2 min (one prompt) |
| 🟠 High | `AuditDisplay.tsx` token re-skin | `TWEAK_FIRST` + `LOW_EFFORT` + `HIGH_IMPACT` + `BLOCKER_FOR_TRACK_B` | 2 min (one prompt) |
| 🟠 High | `ingestion_prompts.md` casing fixup | `TWEAK_FIRST` + `LOW_EFFORT` + `HIGH_IMPACT` + `BLOCKER_FOR_TRACK_A` | 1 min (one prompt) |
| 🟡 Medium | `ValidationDashboard.tsx` decomposition | `TWEAK_FIRST` + Effort 3 + Impact 5 | 10–15 min (one large prompt) |
| ⚪ None | `AppState` enum string-literal tweak | `TWEAK_FIRST` + Effort 1 + Impact 2 | 1 min — defer to C4 |
