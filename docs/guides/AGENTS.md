# Legacy AGENTS Redirect

This file is intentionally minimal.

The authoritative agent instructions for this repository now live at the repo root:

- `AGENTS.md`

For current work, also read the active stage docs referenced there, especially:

- `docs/project/active/figma-agent-tasks.md`
- `docs/project/active/figma-sync-order.json`
- `docs/project/active/frontend-source-of-truth-migration/control/route-matrix.json`

Do not use this file as an independent ruleset. It exists only so older doc indexes and links do not point agents at stale duplicated guidance.
