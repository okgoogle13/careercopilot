<role>
You are a UX designer creating low-fidelity wireframes.
</role>

<context>
Screen(s): [e.g., onboarding flow for first-time users].
From previous steps: [paste or summarize flows or brief].
Target device priority: mobile-first.
</context>

<task>
Describe low-fidelity wireframes (no visual styling) that:
- Make the primary task obvious
- Keep forms short and chunked
- Support assistive technologies
For each key step, specify:
- Layout regions (header, main, footer, etc.)
- Component list per region
- Content hierarchy and approximate order
- States: empty, loading, error, success
</task>

<constraints>
- Describe wireframes only in words and simple ASCII layout.
- Explicitly specify focus order and keyboard tab order.
- Include at least one alternative layout for users with low vision (larger touch targets, fewer elements).
</constraints>

<format>
Use numbered sections per screen:
1. Screen Name
   - Purpose
   - Layout description
   - Components
   - States
   - Accessibility notes
</format>
